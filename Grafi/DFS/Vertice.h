#ifndef VERTICE_H
#define VERTICE_H

#include<iostream>
#include<limits>
#include<string>
using namespace std;

//Questa enum class ci serve per i colori dei grafi
enum class Color{
    WHITE, GRAY, BLACK
};


template<class T>
class Vertice
{
private:
    /* data */
public:
    Vertice(/* args */);
    ~Vertice();
};


#endif
